name: "workflow context"

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    name: My first job
    if: github.repository == 'NityamZWT/Event-booking-System'
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World"
        run: echo "Hello world!"
  goodbye:
    name: My Seconf job
    runs-on: ubuntu-latest
    steps:
      - name: "goodbye world"
        run: echo "GoodBye World!"
